# 生态世界沙盒 · 项目文档

> 上帝视角的种族生态沙盒：程序化世界自运行（繁衍 / 扩张 / 战争 / 兴衰），玩家用天气、地形、疾病等神力干预进程。
> 本文档用于**沉淀已实现内容**，便于后续提新需求时快速建联定位。

---

## 1. 项目简介

| 项 | 说明 |
|---|---|
| 类型 | 上帝视角生态沙盒 / 程序化模拟 |
| 形态 | 单文件 HTML（Canvas + 原生 JS，无依赖，浏览器直接打开） |
| 主程序 | [`生态世界沙盒.html`](生态世界沙盒.html) |
| 核心循环 | `requestAnimationFrame` 驱动；世界自运行 + 玩家实时干预 |
| 设计原型 | 精灵(森林) / 矮人(山地) / 人马(草原) / 鳄鱼(沼泽) 四族 |

### 玩法一句话
程序化生成地图 → 四族小人按 biome 分布并自主游走 → 出生/死亡的人口循环 → 领地扩张 → 接壤战争；玩家随时用 6 种神力改写进程。

---

## 2. 文件清单（项目资产）

| 文件 | 角色 | 说明 |
|---|---|---|
| [`生态世界沙盒.html`](生态世界沙盒.html) | **主程序（可玩原型）** | 地图+小人+生态+战争+神力，全部集成 |
| [`种族小人形象生成器.html`](种族小人形象生成器.html) | 角色生成器 | 像素小人造型/5 动作/朝向/导出 PNG（完整版大图谱） |
| [`游戏地形生成器.html`](游戏地形生成器.html) | 地形生成器 | biome 地图/河流/4 种地图类型/导出 PNG |
| [`种族生态沙盒-数值推演表.xlsx`](种族生态沙盒-数值推演表.xlsx) | 数值推演 | Logistic 人口 / Lotka-Volterra 食物链 / SIR 疾病 / 四族对比 |

> 主程序里的小人是**迷你简化版**；完整 5 动作大图谱在「种族小人形象生成器」里，二者设计语言一致，未来可互换接入。

---

## 3. 快速上手（怎么玩）

1. 浏览器打开 [`生态世界沙盒.html`](生态世界沙盒.html)
2. 顶部第一排：重建世界 / 地图类型 / 每族数量 / 速度 / 河流 / 领地 / 暂停
3. 顶部第二排「神力 ✋」：选一个神力 → 在地图上**点击或按住拖动**施法（笔刷半径可调）
4. HUD 实时显示总人口 + 各族 `当前/承载力`；左上角弹出瘟疫/战争事件

---

## 4. 核心功能模块（10 大模块）

### 4.1 程序化地图
- **噪声**：值噪声 `vnoise` + 5 层 `fbm`（整数哈希 `ihash`）
- **属性**：海拔 `elev` / 湿度 `moist` / 温度 `temp`（温度 = 1 − 纬度×1.45 − 海拔×0.35）
- **地图类型**（海拔衰减规律）：大陆 / 群岛 / 盆地内海 / 超大陆
- **Biome 映射** `biomeOf(e,m,t)`：14 种地貌（见 §6）
- **河流**：高海拔点沿最陡下降流淌入海
- 渲染到**离屏 `bg` canvas**（每帧复用，性能高）

### 4.2 小人（Agent）
- 按各族 **栖息地 biome**（`HAB`）初始放置
- **AI 游走**：`pickTarget` 在 home 附近随机选同 biome 目标 → 直线移动 → 到达休息 → 换目标
- **迷你像素绘制** `drawAgent`：四族差异化剪影 + 行走腿交替 + 身体起伏 + 朝向翻转 + 状态标记（瘟疫红点/饥饿黄点/交战火花）
- 按 y 排序绘制实现**远近遮挡**

### 4.3 人口动态（每 `ECO_INTERVAL=18` 帧）`ecoTick`
- **繁殖**：Logistic `r_eff = r×(1 − n/K)`，新生儿在父母附近同 biome 出生
- **死亡**：年老（age>maxAge）/ 饥饿（人口过载 → hunger 飙升）/ 瘟疫 / 小概率自然
- **承载力 K**：`KCAP[race] = 栖息地 tile 数 × 0.5`
- **总量上限** 650（防卡顿）

### 4.4 瘟疫 `maybePlague`
- 随机偶发（0.5%/eco）选一族感染一批小人，感染期内额外死亡率
- 也可由玩家神力主动施放

### 4.5 领地与扩张
- `computeTerritory`：小人所在格及周围 1 格标为本族领地（后写覆盖 → 接壤区归后者）
- `renderTerritory`：半透明族色叠加层（可开关）
- `expansion`：人口压力 `n/K > 0.8` 时派「开拓者」去 `findFrontier` 找的未占适宜新地 → 领地蔓延

### 4.6 侵略 / 战争（每 10 帧）`combatTick`
- **空间网格**找邻近（<1.1 格）异族小人，35% 概率交战
- **战力** `STR`：矮人 1.5 > 人马 1.2 > 精灵 1.0 > 鳄鱼 0.8；`战力×(0.5~1.5随机)` 定胜负，败者阵亡
- 头顶火花特效；单轮阵亡≥4 弹消息

### 4.7 玩家神力 `applyPower`（鼠标点击/拖动）
| 神力 `power` | 效果 |
|---|---|
| `rain` 降雨 | 湿度↑ → 变森林/绿洲 |
| `raise` 升地 | 海拔↑ → 造山隔断 |
| `water` 挖水 | 海拔↓ → 造河造海隔离 |
| `forest` 造林 | 湿度↑ + 直接变森林 |
| `plague` 瘟疫 | 范围内小人染病 |
| `meteor` 灾祸 | 范围内小人灭杀 + 地表变火山焦土 |
- `forEachTile` 圆形笔刷（半径 `brushR` 1~6）；改地形后 `recalc` 重算 biome + `renderBg`

### 4.8 建筑/聚落系统
- `checkSettlements`：同格 ≥5 同族小人 + 适宜 biome → 生成 1 级聚落；冷却 12 次 `ecoTick`，同族间距 ≥5 格
- 三级聚落：1级小屋（≥5人）→ 2级中房+旗帜（≥9人）→ 3级城堡+塔楼（≥18人）
- 繁殖加成：聚落 4 格内 `rEff × (1 + 0.12 × level)`
- 战损：异族在聚落 1.6 格内交战获胜 → HP -35~-65；HP≤0 时聚落被摧毁
- `meteor` 神力可直接夷平范围内聚落
- `drawSettlement`：像素建筑绘制 + HP<60 红色受损 overlay

### 4.9 实时人口曲线图
- 位置：地图右侧，300px 宽 × 702px 高（与地图同高）
- 每 `ECO_INTERVAL=18` 帧采样一次各族人口，保存最近 `MAX_HIST=300` 个点
- 四族折线（各族 HUD 色），末端带深色底数值标签
- Y 轴动态计算，最小基准 650，向上取整到 50 的倍数

### 4.10 胜负判定 + 事件流面板
- **左侧事件流**：200px 面板，记录最近 15 条事件，带 `分:秒` 时间戳
- **事件分级**：普通(•) / 警告(⚠) / 里程碑(★) / 结局(◆)，四级颜色区分
- **胜负规则**：单族灭绝只记录事件，不结束游戏；**只剩一个种族**时弹出「🏆 世界统一」结局面板
- **结局面板**：存活帧数 / 剩余人口 / 聚落总数 / 四族最终人口；提供「🔄 再来一局」按钮
- **事件埋点**：聚落建立/升级、人口破百、大规模交战、聚落攻陷、瘟疫、灾祸、统一/灭绝

---

## 5. 技术架构

### 5.1 Agent 数据结构
```js
{
  race,            // 'forest'|'dwarf'|'nomad'|'marsh'
  seed,            // 随机相位（动画/造型）
  x, y,            // tile 浮点坐标
  homeI, homeJ,    // 家园 tile（扩张时迁移）
  tx, ty,          // 当前移动目标
  facing,          // 1 右 / -1 左
  moving, rest,    // 移动状态 / 休息计时
  age, hunger,     // 年龄 / 饥饿值
  plague, fight    // 瘟疫计时 / 交战特效计时
}
```

### 5.2 全局状态
```
W=96, H=54, CELL=13              // 地图格子数 / 每格像素
elev[], moist[], temp[]         // 地块属性（长度 W*H）
biome[], river[]                // biome 字符串 / 河流标记
agents[]                        // 所有小人
terr[]                          // 领地归属（0 无主 / 1-4 族）
KCAP{}                          // 各族承载力
bg, terrCv                      // 离屏画布：地图层 / 领地层
settlements[]                   // 聚落数组（含 level/hp）
popHistory[]                    // 人口历史（用于右侧图表）
eventLog[]                      // 事件流历史（左侧面板）
prevPop{}                       // 上轮人口（用于灭绝检测）
gameOver                        // 胜负已分标记
power, brushR, hoverI/J, painting // 神力交互状态
frame                           // 全局帧计数
```

### 5.3 主循环 `tick()`（rAF）
```
if(!paused){
  frame++
  for agent: fight--；rest 或 朝目标移动（facing 跟随）
  frame % 10  === 0 → combatTick()    // 战争
  frame % 18  === 0 → ecoTick()       // 人口/扩张/领地
}
draw()
```

### 5.4 渲染分层 `draw()`
```
drawImage(bg)            // 地图（离屏缓存）
drawImage(terrCv)        // 领地半透明层（可开关）
for s in settlements: drawSettlement(ctx,s)   // 聚落建筑
agents.sort(y).forEach(drawAgent)   // 小人（含动画/状态）
笔刷光标圆圈              // 神力 hover 预览
drawChart()              // 右侧人口曲线图（仅在 ecoTick 后重绘）
```

---

## 6. 关键参数速查表

### 种族参数
| 种族 | key | 栖息地 HAB | 繁殖 r | 寿命 maxAge | 战力 STR | 迷你色 |
|---|---|---|---|---|---|---|
| 精灵(森林) | forest | FOREST, PLAIN | 0.030 | 2600 | 1.0 | #5bbf5a |
| 矮人 | dwarf | MOUNT, HILL | 0.016 | 3200 | **1.5** | #caa36a |
| 人马(游牧) | nomad | GRASS, PLAIN | 0.022 | 2400 | 1.2 | #cf8a4a |
| 鳄鱼(沼泽) | marsh | SWAMP, BEACH | **0.040** | 2000 | 0.8 | #3fa07a |

### Biome 映射 `biomeOf(e,m,t)`（e=海拔 m=湿度 t=温度）
```
e<0.34 → DEEPSEA      e<0.40 → SEA        e<0.435 → BEACH
e>0.90 → SNOW/ROCK(t<0.32)   e>0.78 → MOUNT     e>0.70 → HILL
t<0.26 → SNOW
e<0.50 且 m>0.66 → SWAMP
m<0.24 → DESERT   m<0.46 → GRASS   m<0.68 → PLAIN   否则 → FOREST
```

### 地图类型（海拔衰减，d=离中心归一距离）
| 类型 | 公式 |
|---|---|
| continent 大陆 | e − d×0.55 + 0.12 |
| islands 群岛 | e×1.15 − 0.22 |
| basin 盆地 | e + (d−0.5)×0.55 |
| pangaea 超大陆 | e − d×0.30 + 0.18 |

### 关键时序 / 阈值
| 参数 | 值 | 含义 |
|---|---|---|
| ECO_INTERVAL | 18 帧 | 人口动态周期 |
| 战斗检测 | 每 10 帧 | combatTick |
| 战斗范围 / 概率 | <1.1 格 / 35% | 接壤交战 |
| 承载力系数 | ×0.5 | KCAP = 栖息地 tile × 0.5 |
| 扩张阈值 | n/K > 0.8 | 触发开拓者 |
| 总人口上限 | 650 | 防卡顿 |
| 笔刷半径 | 1~6 | brushR |
| 聚落生成人口 | ≥5 同族/格 | 触发新聚落 |
| 聚落生成冷却 | 12 次 ecoTick | 约 216 帧 ≈ 3.6s |
| 聚落最小间距 | 5 格 | 同族聚落之间 |
| 聚落繁殖加成 | +12%/lv | 1级+12%，3级+36% |
| 聚落战损 | 35~65 HP/次 | 异族交战获胜 |
| 图表历史容量 | 300 点 | 约 90 秒 @60fps |
| 事件流容量 | 15 条 | 左侧面板滚动 |
| 统一胜利条件 | 只剩 1 个种族 | 其他三族全部灭绝 |

---

## 7. 代码结构索引（函数 → 职责）

| 分组 | 函数 | 职责 |
|---|---|---|
| 噪声 | `ihash` `vnoise` `fbm` | 整数哈希值噪声 + 分形叠加 |
| 地图 | `biomeOf` | 属性→biome 映射 |
| | `generate` | 生成 elev/moist/temp/biome/river |
| | `renderBg` / `shade` | 地图渲染到离屏 / 海拔明暗 |
| 小人 | `newAgent` `initAgents` | 创建/初始化小人 |
| | `pickTarget` | AI 选目标 |
| | `drawAgent` | 迷你像素小人绘制（含动画/状态） |
| 人口 | `ecoTick` | 繁殖/死亡总调度 |
| | `spawnNear` `maybePlague` | 生育/瘟疫 |
| | `updateHud` `flashMsg` | HUD / 事件提示 |
| 领地 | `computeTerritory` `renderTerritory` | 计算/渲染领地 |
| | `findFrontier` `expansion` | 找新地/派开拓者 |
| 战争 | `combatTick`（+`STR`） | 接壤交战结算 |
| 聚落 | `checkSettlements` `cleanupSettlements` | 生成/清理聚落 |
| | `drawSettlement` | 聚落像素绘制 |
| 图表 | `drawChart` | 右侧人口折线图绘制 |
| 事件 | `pushEvent` `renderEventLog` | 压入事件/渲染面板 |
| | `checkVictory` `showGameOver` | 胜负判定/结局面板 |
| 神力 | `recalc` `forEachTile` `applyPower` | 地块重算/笔刷/施法 |
| 循环 | `draw` `tick` | 渲染分层 / 主循环 |
| 入口 | 末尾事件绑定 | 神力工具栏 + 鼠标事件 + `rebuild()` `tick()` |

---

## 8. 数值模型对应（理论 → 代码）

| 数值模型（见 xlsx） | 代码实现 |
|---|---|
| Logistic 人口增长 | `ecoTick` 繁殖 `r×(1−n/K)` |
| 环境容量 K | `KCAP` = 栖息地 tile × 0.5 |
| SIR 疾病 | `plague` 计时 + 死亡率（简化版） |
| 压力-扩张 | `expansion` 的 `n/K>0.8` |
| Lanchester 战斗 | `combatTick` 的 `STR` 战力对比（简化版） |
| 温度对繁殖 M_temp | （暂未接入，见 §9 扩展点） |

---

## 9. 已实现 vs 可扩展

### ✅ 已实现
- [x] 程序化地图（14 biome / 河流 / 4 地图类型）
- [x] 四族小人（迷你造型 / 行走动画 / 朝向 / 状态标记）
- [x] 按 biome 分布 + 自主游走 AI
- [x] 人口动态（繁殖 / 饥饿 / 年老 / 瘟疫）
- [x] 领地扩张（领地可视化 + 开拓者）
- [x] 侵略战争（空间网格 + 战力系统）
- [x] 玩家神力（降雨/升地/挖水/造林/瘟疫/灾祸）
- [x] 建筑/聚落系统（三级聚落 / 繁殖加成 / 战损摧毁）
- [x] 实时人口曲线图（右侧折线 / 300点历史 / 动态Y轴）
- [x] 胜负判定（灭绝制胜利 / 中央结局面板 / 再来一局）
- [x] 事件流面板（左侧15条 / 四级事件分级 / 全埋点）

### ⏳ 待扩展（常见下一步）
- [ ] 接入完整 5 动作大图谱小人（替换迷你版）
- [ ] 温度/食物链对繁殖的影响（接入 M_temp、Lotka-Volterra）
- [ ] 存档/分享（导出 seed + 状态）
- [ ] 季节/天气周期（全局气候循环）
- [ ] 音效（事件分级预埋 Audio()）
- [ ] 建筑/科技/外交等深度系统
- [ ] 接入完整 5 动作大图谱小人（替换迷你版）
- [ ] 打包为 Godot/Unity 可发布版本

---

## 10. 如何提新需求（建联指引）⭐

> 未来提需求时，**附上本文档路径**并说明诉求，即可快速定位到要改的模块/函数。

### 提需求模板
```
参考《生态世界沙盒-项目文档.md》，我想给 [生态世界沙盒.html] 新增/修改：
- 诉求：______（如"小人会建房子"、"加食物资源"、"季节变化"）
- 期望表现：______
```

### 常见需求 → 改动定位速查
| 你想做的事 | 主要改动位置 |
|---|---|
| 调种族强弱/繁殖/寿命 | `RACE_PARAMS` / `STR` / `HAB` |
| 改地图地貌/海陆比例 | `biomeOf` 阈值 / 地图类型衰减 |
| 改小人长相/动画 | `drawAgent`（或接入大图谱） |
| 改人口/死亡规则 | `ecoTick` |
| 改战争规则 | `combatTick` / `STR` |
| 改领地/扩张逻辑 | `computeTerritory` / `expansion` / `findFrontier` |
| 新增一种神力 | `applyPower` 加分支 + 工具栏加按钮 |
| 加新种族 | `HAB`/`RACE_NAME`/`RACE_PARAMS`/`STR`/`RACE_IDX`/`TERR_COL`/`drawAgent` 同步加 |
| 加 HUD/图表/面板 | `updateHud` + HTML 容器 |
| 加聚落/建筑逻辑 | `checkSettlements` / `drawSettlement` / 升级判定 |
| 加图表/曲线 | `drawChart` + `popHistory` 采样 |
| 加事件流/胜负 | `pushEvent` / `checkVictory` / `showGameOver` |
| 加新地块属性（如资源） | 新增全局数组 + `generate` 填充 + `ecoTick` 消费 |

### 接入新种族的完整清单（避免遗漏）
新增一族需同步更新：`HAB`、`RACE_NAME`、`RACE_PARAMS`、`STR`、`RACE_IDX`、`TERR_COL`、`RACE_COL`、`drawAgent` 的 switch 分支、HUD pop 统计对象。

### 技术约束备忘
- 单文件、无依赖、原生 Canvas；保持这一形态便于直接打开调试
- 性能敏感处：`combatTick`（空间网格已优化）、`renderBg`（仅地形变更时重渲染）
- 坐标体系：小人用 **tile 浮点坐标**，绘制时 ×`CELL`；鼠标用 `getBoundingClientRect` 换算（适配缩放）

---

---

## 11. 今日新增内容速查（2026-06-06）

> 以下内容为快速索引，与 §4 一一对应。

### 11.1 聚落系统关键函数

| 函数 | 调用位置 | 职责 |
|---|---|---|
| `checkSettlements()` | `ecoTick` 末尾 | 检测生成新聚落 + 动态升级 |
| `cleanupSettlements()` | `ecoTick` 末尾 | 过滤 HP≤0 的聚落 |
| `drawSettlement(ctx,s)` | `draw()` 中 agents 之前 | 绘制三级像素建筑 + 受损特效 |

### 11.2 图表系统关键函数

| 函数 | 调用位置 | 职责 |
|---|---|---|
| `drawChart()` | `ecoTick` 末尾 | 绘制右侧人口折线图 |

### 11.3 事件与胜负关键函数

| 函数 | 调用位置 | 职责 |
|---|---|---|
| `pushEvent(type,text)` | 各事件埋点 | 压入事件并刷新左侧面板 |
| `renderEventLog()` | `pushEvent` 内部 | 渲染 `#eventList` |
| `checkVictory()` | `ecoTick` 末尾 | 检测灭绝/统一/里程碑 |
| `showGameOver(race,pop)` | `checkVictory` 内 | 弹出中央结局面板 + 暂停 |

### 11.4 事件埋点速查

| 埋点位置 | 事件内容 | 级别 |
|---|---|---|
| `checkSettlements` — 建立 | XX 建立了新聚落 | 普通 |
| `checkSettlements` — 升级 | XX 聚落升至 2/3 级 | 里程碑 |
| `ecoTick` | XX 人口突破 100 | 里程碑 |
| `combatTick` — 大规模交战 | 领地交战，N 人阵亡 | 警告 |
| `combatTick` — 聚落战损 | XX 的聚落被攻陷 | 警告 |
| `maybePlague` | XX 爆发瘟疫，感染 N 人 | 警告 |
| `applyPower(meteor)` — 灭人 | 天降灾祸，N 人罹难 | 警告 |
| `applyPower(meteor)` — 夷平聚落 | XX 的聚落被灾祸夷平 | 警告 |
| `checkVictory` — 灭绝 | XX 已灭绝 | 结局（仅记录） |
| `checkVictory` — 统一 | XX 统一了世界！ | 结局（弹窗） |

### 11.5 技术约束更新
- 单文件、无依赖、原生 Canvas 形态保持不变
- 新增性能敏感处：`drawChart()` 仅在 `ecoTick` 时重绘（每 18 帧），不每帧绘制
- 布局采用三栏 flex：左侧事件流(200px) + 中间地图(1248px) + 右侧图表(300px)

---

---

## 12. 更新日志（2026-06-06）

> 以下为本日新增/修改内容，与 §4 / §5 / §6 / §7 一一对应。

### 12.1 领地边界线

- 在 `renderTerritory()` 中**新增边界线绘制**：每个领地格子与无主地/异族地的交界处，绘制 2px 粗、90% 透明度的实色边界线；同族内部相邻边不绘制，避免杂乱。
- 保留原有的 30% 透明度填充，确保底层地貌可见。

### 12.2 领地系统重构（持久化）

将原 `computeTerritory()`（每帧 `terr.fill(0)` 清空后重算）重构为**持久化领地系统**：

| 变更项 | 说明 |
|---|---|
| **实时标记** | `tick()` 每帧小人移动后，立即将小人所在格及周围 1 格标记为本族领地（覆盖异族） |
| **持久化** | `terr` 数组不再每帧清空，走过的地方**永久保留** |
| **覆盖机制** | 异族小人经过时直接覆盖领地归属（后写覆盖） |
| **无主化规则** | 新增 `decayTerritory()`：每 `ECO_INTERVAL`（18 帧）检测一次；若某领地格子**无同族小人**且**上/下/左/右四邻居都不是同族领地**，则该格子置为无主（0） |
| **初始化** | `initAgents()` 中 `terr.fill(0)` 清空领地，由小人从脚下实时重新生长 |

### 12.3 种族颜色优化

将四大种族颜色更新为**更高饱和度、与自然地貌区分度更强**的配色，同步更新 `TERR_COL`、`RACE_COL`、`drawAgent`（小人像素）、`buildLegend`（图例）：

| 种族 | 旧颜色 | 新颜色 | 说明 |
|---|---|---|---|
| 精灵 | `#5bbf5a` | `#00e676` 亮翠绿 | 比森林绿更亮更偏青，在自然背景中非常跳脱 |
| 矮人 | `#caa36a` | `#ff9100` 亮橙 | 类似熔岩/琥珀，与山地灰、沙漠黄明显区分 |
| 人马 | `#cf8a4a` | `#ff1744` 亮玫红 | 与所有自然地貌强烈对比，极具侵略感 |
| 鳄鱼 | `#3fa07a` | `#00b0ff` 亮青蓝 | 与沼泽深绿、水域蓝区分明显，冷色调突出 |

### 12.4 代码结构更新

| 分组 | 函数 | 职责变更 |
|---|---|---|
| 领地 | `computeTerritory()` → **`decayTerritory()`** | 由"每帧清空重算"改为"无主化衰减检测" |
| 领地 | `tick()` 内小人循环 | 新增实时领地标记逻辑（3×3 笔刷覆盖） |
| 入口 | `initAgents()` | 末尾改为 `terr.fill(0); renderTerritory();` |

### 12.5 技术约束更新
- 单文件、无依赖、原生 Canvas 形态保持不变
- `terr` 数组持久化后内存占用不变（仍为 `W*H` 整数数组）
- `decayTerritory()` 每 18 帧遍历 5184 格 + Set 查询，性能开销可忽略

---

_最后更新：2026-06-06，随主程序 [`生态世界沙盒.html`](生态世界沙盒.html) 当前实现。_


---

## 13. 今日更新记录（2026-06-08）

> 以下为本日新增/修改内容，包含**种族体系重构**与**战斗升级系统**两大模块。

---

### 13.1 种族体系重构：五族时代

#### 决策记录
| 决策项 | 选项 | 说明 |
|---|---|---|
| 鳄鱼替代 | **海妖** | 保留沼泽+海滩栖息地，高繁殖低战力，契合水生设定 |
| 新增种族 | **人类** | 全能适应（平原/草原/森林），金黄色 `#ffea00` |
| 同步修改 | 形象生成器 | `种族小人形象生成器.html` 同步新增海妖+人类像素图谱 |

#### 五族最终参数

| 种族 | key | 栖息地 | 繁殖 r | 寿命 | 战力 STR | 颜色 |
|---|---|---|---|---|---|---|
| 精灵 | forest | FOREST, PLAIN | 0.030 | 2600 | 1.0 | `#00e676` |
| 矮人 | dwarf | MOUNT, HILL | 0.016 | 3200 | **1.5** | `#ff9100` |
| 人马 | nomad | GRASS, PLAIN | 0.022 | 2400 | 1.2 | `#ff1744` |
| **海妖** | **sea** | **SWAMP, BEACH** | **0.040** | **2000** | **0.8** | **`#00b0ff`** |
| **人类** | **human** | **PLAIN, GRASS, FOREST** | **0.028** | **2800** | **1.1** | **`#ffea00`** |

#### 代码改动索引（生态世界沙盒.html）

| # | 位置 | 改动 |
|---|---|---|
| 1 | `const HAB` | `marsh`→`sea`，新增 `human` |
| 2 | `const RACE_NAME` | 改名+新增 |
| 3 | `let prevPop` | 更新键名 |
| 4 | `const RACE_IDX` | `sea:4, human:5` |
| 5 | `const TERR_COL` | 新增第6位 `#ffea00` |
| 6 | `const RACE_PARAMS` | 海妖/人类参数 |
| 7 | `const RACE_COL` | 海妖/人类颜色 |
| 8 | `const STR` | 海妖0.8，人类1.1 |
| 9 | `drawAgent` | 海妖鳍冠鳞身，人类金发战衣 |
| 10 | `buildLegend` | 五族图例 |
| 11 | 各处 `pop` 对象 | 全部五族化 |
| 12 | `drawChart` | 五族折线 |
| 13 | `showGameOver` | 五族统计文本 |

#### 形象生成器改动索引（种族小人形象生成器.html）

| # | 位置 | 改动 |
|---|---|---|
| 1 | `const MARSH` → `const SEA` | 新像素图谱：鳍冠·鳞身·触手尾，W16 |
| 2 | 新增 `const HUMAN` | 新像素图谱：金发·战衣·全能，W14 |
| 3 | `const RACES` | `marsh`→`sea`，新增 `human` 配色参数 |
| 4 | `.legend` 说明 | 更新为五族描述 |

---

### 13.2 战斗升级系统（新增大模块）

#### 13.2.1 设计目标
解决原战斗系统三大问题：
1. **一击必杀无翻盘** → 引入等级战力乘数，老兵可逆袭
2. **无个体成长** → 击杀积累经验，晋升变强
3. **击杀无反馈** → 连斩奖励+升级事件+王牌单位追踪

#### 13.2.2 四阶晋升体系

| 等级(rank) | 精灵 | 矮人 | 人马 | 海妖 | 人类 | 战力乘数 |
|:---:|:---|:---|:---|:---|:---|:---:|
| **0** | 嫩芽 | 学徒 | 幼驹 | 幼体 | 民兵 | ×1.0 |
| **1** | 游侠 | 锻铁者 | 风行者 | 深潜者 | 骑士 | **×1.4** |
| **2** | 古树守卫 | 战争领主 | 战蹄酋长 | 潮汐领主 | 军团长 | **×1.9** |
| **3** | 翡翠君王 | 山王 | 草原霸主 | 潮汐之母 | 至高统帅 | **×2.6** |

> **称号命名原则**：各族独立，霸气但不越界（禁用"神"字），4字以内便于HUD显示。

#### 13.2.3 战力公式重构

```js
// 旧
const sa = STR[a.race] * (0.5 + Math.random());
// 新
const sa = STR[a.race] * RANK_MULT[a.rank] * (0.5 + Math.random()) * auraBonus;
```

**质变示例**：传说精灵翡翠君王（1.0×2.6=2.6）可正面碾压普通矮人（1.5×1.0=1.5）。

#### 13.2.4 击杀经验矩阵

基础XP = 矩阵值 × `STR[目标种族]`（种族价值系数）

| 击杀者＼目标 | 0阶 | 1阶 | 2阶 | 3阶 |
|:---:|:---:|:---:|:---:|:---:|
| **0阶** | 1 | 2 | 5 | 10 |
| **1阶** | 1 | 1 | 3 | 6 |
| **2阶** | 1 | 1 | 1 | 3 |
| **3阶** | 1 | 1 | 1 | 1 |

> 越级击杀重奖，虐菜无收益。击杀传说获得10XP（但传说极难死）。

#### 13.2.5 连斩奖励

30帧内连续击杀：
- 2杀：XP ×1.3
- 3杀：XP ×1.6
- 4杀+：XP ×2.0

事件示例：`🔥 矮人山王连斩4人！`

#### 13.2.6 高等级限制机制（核心设计）

| 等级 | 限制类型 | 规则 |
|:---:|:---|:---|
| **3阶 传说** | **硬性唯一制** | 每族最多1个；需满足：① 本族无传说 ② 击杀过敌方英雄 ③ 本族有≥2级聚落 |
| **2阶 英雄** | **人口比例软限制** | ≤ 该族总人口15%；超限后勇士XP获取-50% |
| **1阶 勇士** | **人口比例软限制** | ≤ 该族总人口50%；超限后学徒XP获取-30% |
| **0阶 学徒** | 不限 | 新生儿默认学徒 |

> 软限制通过"瓶颈"实现：不硬卡数量，但超额后升级效率暴跌，自然淘汰后自动回落。

#### 13.2.7 配套机制

| 机制 | 效果 |
|:---|:---|
| **英雄光环** | 周围2格友军战力 **+8%** |
| **传说光环** | 周围3格友军战力 **+15%** |
| **聚落战损增强** | 英雄/传说攻击聚落时，伤害额外 **+25%~75%** |
| **繁殖惩罚** | 高等级单位繁殖率 **-8%×rank**（精英-8%，队长-16%，传说-24%） |
| **传说陨落事件** | 传说死亡 → ⚠️ 警告事件「XX族山王陨落！」 |

#### 13.2.8 视觉标识（drawAgent）

在13像素CELL内，于头部上方绘制金色标记：

| 等级 | 标识 | Y坐标 |
|:---:|:---|:---:|
| 勇士 | 金色细横线 `#ffd700` | `y=-18+bob` |
| 英雄 | 金色方块 `#ffd700` | `y=-19+bob` |
| 传说 | 金色皇冠 `#ffd700`+亮顶 `#ffea00` | `y=-20~-21+bob` |

> 交战火花在 `y=-16`，瘟疫在 `y=-15`，等级标记在更上方，**互不重叠**。

#### 13.2.9 新增事件埋点

| 事件内容 | 级别 | 触发条件 |
|:---|:---:|:---|
| XX族晋升游侠/锻铁者/… | 里程碑 ★ | 首次晋升1阶 |
| XX族晋升古树守卫/战争领主/… | 里程碑 ★ | 首次晋升2阶 |
| XX族晋升翡翠君王/山王/… | 里程碑 ★ | 首次晋升3阶（传说诞生） |
| XX族山王连斩N人！ | 警告 ⚠️ | streak≥3 |
| XX族山王陨落！ | 警告 ⚠️ | 传说死亡 |
| XX族军团长阵亡 | 警告 ⚠️ | 英雄死亡 |

#### 13.2.10 代码改动索引

| 分组 | 新增/修改 | 职责 |
|---|---|---|
| 常量 | `RACE_RANK_NAMES` | 各族四级称号二维对象 |
| 常量 | `RANK_MULT` | 战力乘数 [1.0, 1.4, 1.9, 2.6] |
| 常量 | `RANK_XP_NEED` | 升级阈值 [0, 3, 8, 15] |
| 常量 | `KILL_XP_BASE` | 4×4经验矩阵 |
| 常量 | `RANK_LIMIT_PCT` | 人口比例限制 [1.0, 0.5, 0.15, 0.03] |
| 工具 | `getKillXP(w,lR,lRace)` | 计算单次击杀经验 |
| 工具 | `getRankCount(race,rank)` | 统计某族某等级人数 |
| 工具 | `getRacePop(race)` | 统计某族总人口 |
| 工具 | `canPromote(a)` | 晋升资格判定（含全部限制逻辑） |
| 小人 | `newAgent` | 新增 `rank/xp/kills/streak/lastKillFrame/hasHeroKill` |
| 战争 | `combatTick` | **完全重写**：光环预计算→战力公式→经验结算→连斩→升级判定→聚落战损→死亡事件 |
| 小人 | `drawAgent` | 新增等级金色标识绘制 |
| 人口 | `ecoTick` | 繁殖公式加入 `rankPenalty=1-rank×0.08` |

---

### 13.3 技术约束更新

- 单文件、无依赖、原生 Canvas 形态保持不变
- `combatTick` 新增光环预计算：遍历空间网格（范围-2~+2），每帧额外开销 ≈ 战斗agent数 × 25格查询，因空间网格已优化，性能影响可忽略
- Agent 对象新增 6 个字段（`rank/xp/kills/streak/lastKillFrame/hasHeroKill`），内存占用微增（6 × 4字节 × agent数 ≤ 650 × 24 ≈ 15KB）
- 战力乘数使高等级单位显著强于普通单位，可能导致局部战斗一边倒 → 已通过**数量限制**和**繁殖惩罚**平衡

---

### 13.4 后续扩展建议（预留接口）

以下机制在本次更新中已预留数据结构，但未完全实现，可作为下一步：

| 建议功能 | 当前预留 | 实现难度 |
|:---|:---|:---:|
| 传说死亡士气打击 | `hasHeroKill` / 传说死亡事件已埋点， morale 字段可扩展 | 中 |
| 等级分布HUD | `getRankCount()` 工具函数已可用，可直接接入 `updateHud` | 低 |
| 高等级单位追踪/王牌榜 | `kills` / `streak` 字段已记录 | 低 |
| 击杀回放/精彩时刻 | 事件流已记录连斩，可扩展为独立面板 | 中 |
| 等级与存档系统 | `rank/xp` 已纳入 agent 数据结构 | 中 |

---

_最后更新：2026-06-08，随主程序 [`生态世界沙盒.html`](生态世界沙盒.html) 与 [`种族小人形象生成器.html`](种族小人形象生成器.html) 当前实现。_


---

## 14. 功能微调（2026-06-08 追加）

### 14.1 领地边界线改为可选显示

#### 需求
原领地系统会同时绘制「半透明填充」和「2px边界线」。边界线在高密度领地时视觉上较杂乱，需改为**默认隐藏、可手动切换**。

#### 实现方式
| 控制方式 | 操作 |
|---|---|
| **UI 复选框** | 顶部控制栏勾选「边界线」 |
| **快捷键** | 按键盘 **B** 键切换 |

#### 代码改动

| 位置 | 改动 |
|---|---|
| HTML 控制栏 | 新增 `<input type="checkbox" id="border"> 边界线` |
| 全局状态 | 新增 `let showBorder=false;` |
| `renderTerritory()` | 边界线绘制代码包入 `if(showBorder){...}` |
| 事件绑定 | `borderCb.onchange` + 键盘 `B` 监听，两者状态同步 |

#### 行为
- 领地填充层仍受原「领地」复选框控制
- 边界线默认隐藏，开启后即时重绘 `terrCv`
- 切换时弹出 `flashMsg`：「🔲 显示领地边界线」/「🔲 隐藏领地边界线」

---

_最后更新：2026-06-08，随主程序 [`生态世界沙盒.html`](生态世界沙盒.html) 当前实现。_


---

## 15. 种族平衡调整（2026-06-08 追加）

### 15.1 问题诊断

实测反馈：矮人太弱，人马和人类太强。

| 种族 | 原问题 |
|:---|:---|
| 矮人 | 繁殖率最低(0.016)+山地面积小+寿命长但来不及发挥 |
| 人马 | 栖息地广(草原+平原)+战力1.2+繁殖0.022，综合过强 |
| 人类 | 栖息地最广(平原+草原+森林)+高繁殖+高战力，无明显短板 |

### 15.2 调整方案：主场优势 + 参数微调

#### 新增机制：主场优势
每个种族在**适宜栖息地**内战斗时获得战力加成：

| 种族 | 主场栖息地 | 加成 |
|:---|:---|:---:|
| 精灵 | 森林/平原 | +20% |
| 矮人 | 山地/丘陵 | **+25%** |
| 人马 | 草原/平原 | +15% |
| 海妖 | 沼泽/海滩 | +15% |
| 人类 | 平原/草原 | +5%（全能但无强主场） |

> 矮人在山地作战基础战力：1.5 × 1.25 = **1.875**；传说山王配合等级乘数可达 1.5 × 2.6 × 1.25 = **4.875**。

#### 参数调整

| 种族 | 繁殖率 r | 战力 STR | 栖息地 | 承载力系数 |
|:---|:---:|:---:|:---|:---:|
| 精灵 | 0.030 → **0.028** | 1.0（不变） | 森林/平原 | 0.5 |
| 矮人 | 0.016 → **0.020** | 1.5（不变） | 山地/丘陵 | **0.7** |
| 人马 | 0.022 → **0.020** | 1.2 → **1.1** | 草原/平原 | 0.5 |
| 海妖 | 0.040（不变） | 0.8（不变） | 沼泽/海滩 | 0.5 |
| 人类 | 0.028 → **0.025** | 1.1 → **1.0** | 平原/草原（去掉森林） | 0.5 |

### 15.3 预期效果

- **矮人**：人口增长加快、山地防守极强、不易早期被推平
- **人马**：扩张速度放缓、单体战力下降，不再全面碾压
- **人类**：全能但无明显优势，需靠数量和聚落取胜
- **精灵**：数量优势稍减，森林主场+高繁殖仍然强势
- **海妖**：基本不变，继续靠数量人海战术

### 15.4 代码改动索引

| 位置 | 改动 |
|---|---|
| `const HAB` | 人类去掉 FOREST |
| `const RACE_PARAMS` | 五族繁殖率调整 |
| `const STR` | 人马/人类战力下调 |
| 新增 `HOME_BONUS` | 主场战力加成表 |
| 新增 `KCAP_MULT` | 矮人承载力系数0.7 |
| `initAgents` KCAP计算 | 使用 KCAP_MULT |
| `combatTick` 战力公式 | 加入 `homeA` / `homeB` 主场加成 |

---

_最后更新：2026-06-08，随主程序 [`生态世界沙盒.html`](生态世界沙盒.html) 当前实现。_


---

## 16. BOSS系统（2026-06-08 追加）

### 16.1 需求确认

| 项目 | 决策 |
|---|---|
| 世界BOSS | 无差别主动攻击，击败获得纯战力buff |
| 种族BOSS | 种族内部守护，被动防御反击，击败获得神器 |
| 神器持续时间 | 90秒 |
| 重生机制 | 可重生，世界BOSS每局1-2次，间隔90秒；种族BOSS死亡后120秒重生 |

### 16.2 世界BOSS：深渊巨兽

- **生成时机**：游戏开始 **60秒** 后首次生成，之后每隔 **90秒** 再生成一次，每局最多 **2次**
- **生成位置**：地图危险地带（深海/雪原/岩石/火山）
- **外观**：紫色巨型方块（2×2格子），顶部血条
- **行为**：
  - 自动向最近的单位移动
  - 攻击范围内（2.5格）每15帧触发一次范围秒杀
  - 对范围内所有单位一击必杀
- **HP**：300点
- **击败奖励**：击败种族获得 **「世界意志」buff**
  - 全族战力 **+25%**
  - 持续 **90秒**

> 设计意图：制造全图危机，弱者可能被迫合力对抗，强者抢最后一刀。

### 16.3 种族BOSS：守护神殿

- **生成时机**：游戏开始 **30秒** 后，每族主栖息地深处自动生成
- **外观**：各族颜色巨型方块（2×2格子），白色边框，顶部血条
- **行为**：
  - 不可移动
  - 被动防御：被异族攻击后激活反击
  - 反击范围2.5格，对普通单位秒杀，对英雄/传说有50%存活率
  - 同族单位靠近时回血（+0.5 HP/帧）
- **HP**：150点
- **重生**：被击败后 **120秒** 原地重生

| 种族 | 守护神殿 | 被夺神器 |
|:---|:---|:---|
| 精灵 | 翡翠古树 | **翡翠之弓**：攻击范围+1格 |
| 矮人 | 山王祭坛 | **山王战锤**：对聚落伤害+100% |
| 人马 | 风之图腾 | **风之蹄**：移动速度+50% |
| 海妖 | 潮汐神殿 | **潮汐宝珠**：水域/沼泽战斗+40% |
| 人类 | 王权王座 | **王权剑**：击杀经验翻倍 |

### 16.4 HP机制

BOSS采用独立的HP制，普通单位仍保持一击必杀：

```js
function damageBoss(boss, attacker){
  boss.hp -= Math.max(1, 1 + attacker.rank);  // 高等级单位伤害更高
  if(boss.hp <= 0) onBossDefeated(boss, attacker.race);
}
```

### 16.5 代码改动索引

| 分组 | 新增/修改 | 职责 |
|---|---|---|
| 全局状态 | `worldBoss`, `raceBosses`, `worldBuff`, `activeArtifacts` | BOSS与奖励状态 |
| 常量 | `MAX_WORLD_BOSS`, `WORLD_BOSS_INTERVAL`, `WORLD_BOSS_HP`, `RACE_BOSS_HP`, `RACE_BOSS_RESPAWN`, `ARTIFACT_DURATION`, `ARTIFACTS` | BOSS参数与神器表 |
| 小人 | `newAgent` | 新增 `_bossKill` 标记字段 |
| 主循环 | `tick()` | 每10帧调用 `bossTick()`，应用风之蹄速度加成 |
| 战争 | `combatTick()` | 加入与BOSS战斗逻辑，战力公式中加入世界BUFF/潮汐宝珠加成，经验结算加入王权剑，攻击范围加入翡翠之弓，聚落战损加入山王战锤 |
| 新增函数 | `createWorldBoss()` | 在危险地带生成世界BOSS |
| 新增函数 | `createRaceBoss(race)` | 在主栖息地深处生成种族BOSS |
| 新增函数 | `damageBoss(boss, attacker)` | BOSS受到伤害与死亡判定 |
| 新增函数 | `onBossDefeated(boss, winnerRace)` | 发放BUFF/神器奖励，事件埋点 |
| 新增函数 | `bossTick()` | BOSS生成、移动、攻击、回血、反击、重生、过期清理 |
| 新增函数 | `drawBoss(ctx, boss)` | 绘制BOSS方块与血条 |
| 渲染 | `draw()` | 绘制BOSS（最上层） |
| HUD | `updateHud()` | 显示世界意志/神器倒计时 |
| 重建 | `rebuild()` | 重置所有BOSS状态 |

### 16.6 事件埋点

| 事件内容 | 级别 | 触发条件 |
|:---|:---:|:---|
| 深渊巨兽降临世界！ | 里程碑 ★ | 世界BOSS生成 |
| XX族击败了深渊巨兽，获得世界意志！ | 里程碑 ★ | 世界BOSS被击败 |
| XX族击败了YY族的守护神殿，夺得ZZ！ | 里程碑 ★ | 种族BOSS被击败 |
| XX族的守护神殿已苏醒 | 普通 · | 种族BOSS重生 |
| 世界意志buff已消散 | 普通 · | 世界BUFF过期 |
| XX族的神器力量已消散 | 普通 · | 神器过期 |

### 16.7 注意事项

- 深渊巨兽可能一次性秒杀大量单位，建议观察其生成位置，避免在世界BOSS附近集结人口
- 种族BOSS被同族包围时会持续回血，异族需要集中火力才能击败
- 多个神器可以同时存在（由不同种族持有），但只有获胜族能享受神器效果
- 玩家可用神力（如陨石）直接对世界BOSS附近造成区域伤害，但BOSS本身不受神力影响

---

_最后更新：2026-06-08，随主程序 [`生态世界沙盒.html`](生态世界沙盒.html) 当前实现。_


---

## 17. BOSS生成位置优化（2026-06-08 追加）

### 17.1 问题

实测发现BOSS生成位置不合理：

| BOSS | 原问题 |
|:---|:---|
| 世界BOSS | 生成在深海/海里或偏远角落，远离人口，需要走很久才能参战 |
| 种族BOSS | 选择"离中心最远"的主栖息地，容易选到边境孤立小块，开局就被异族发现 |

### 17.2 优化方案

#### 世界BOSS：中心附近的陆地危险地带

**原则**：一出场就能介入战斗，不泡在海里。

| 优化项 | 说明 |
|---|---|
| 候选地形 | `SNOW` / `ROCK` / `VOLCANO` / `MOUNT` / `HILL`（陆地危险地带） |
| 候选范围 | 地图中心 35% 范围内优先 |
| 选择策略 | 按距离中心排序，取前 50%，再随机一个 |
| Fallback | 中心附近没有时，扩大到全图陆地危险地带 |

#### 种族BOSS：栖息地内部 + 大后方

**原则**：必须在主栖息地连续块深处，避免边境孤立点。

| 优化项 | 说明 |
|---|---|
| 候选格子 | 主栖息地（HAB[race][0]）的所有格子 |
| 内部筛选 | 8邻居中 ≥6 个是同族，确保在栖息地深处 |
| 选择策略 | 在内部点中按离中心距离降序，取最远 30%（大后方），再随机一个 |
| Fallback | 没有内部点时，退回到所有适宜地 |

### 17.3 预期效果

- 世界BOSS生成后 **30秒内** 必然进入战场，形成有效威胁
- 种族BOSS位于栖息地腹地，该族有足够时间和空间组织防守
- 异族需要主动深入敌境才能挑战种族BOSS，增加战略纵深

### 17.4 代码改动索引

| 函数 | 改动 |
|---|---|
| `createWorldBoss()` | 完全重写：中心附近陆地危险地带 + 排序取前50% + 随机 |
| `createRaceBoss(race)` | 完全重写：内部筛选 + 大后方排序 + 随机 |

---

_最后更新：2026-06-08，随主程序 [`生态世界沙盒.html`](生态世界沙盒.html) 当前实现。_


---

## 18. 种族BOSS生成位置再优化：人口最密集处（2026-06-08 追加）

### 18.1 需求

种族BOSS应出现在该族人口最密集的地方，而不是固定的栖息地深处。这样更符合"守护族群核心"的设定，也更容易被该族自然防守。

### 18.2 新规则

| 优先级 | 规则 | 条件 |
|:---|:---|:---|
| **第一优先** | 生成在该族人口最密集的格子 | 该族有≥3个单位，且最密集格子有≥3个单位 |
| **Fallback** | 生成在主栖息地内部+大后方 | 人口不足或分布过于分散 |

### 18.3 实现方式

```js
const raceAgents=agents.filter(a=>a.race===race);
if(raceAgents.length>=3){
  const posCount=new Map();
  let bestKey=null, bestCount=0;
  for(const a of raceAgents){
    const i=Math.round(a.x), j=Math.round(a.y);
    const key=i+'_'+j;
    const c=(posCount.get(key)||0)+1;
    posCount.set(key,c);
    if(c>bestCount){ bestCount=c; bestKey=[i,j]; }
  }
  if(bestKey && bestCount>=3){
    // 在人口最密集格生成种族BOSS
  }
}
```

### 18.4 效果

- 种族BOSS会动态跟随族群中心移动
- 开局30秒生成时，BOSS出现在该族当前最热闹的区域
- 如果该族已经被打散（人口<3或最密格子<3人），则退回到安全的大后方
- 重生时也会重新计算当前人口中心

### 18.5 代码改动索引

| 函数 | 改动 |
|---|---|
| `createRaceBoss(race)` | 新增人口密度检测逻辑，作为第一优先级；原栖息地内部逻辑作为fallback保留 |

---

_最后更新：2026-06-08，随主程序 [`生态世界沙盒.html`](生态世界沙盒.html) 当前实现。_


---

## 19. 种族BOSS生成时机优化（2026-06-08 追加）

### 19.1 问题

实测发现：种族BOSS生成时，附近已经没有相同种族的单位，导致BOSS孤零零地出现在空地上，失去"守护族群"的意义。

### 19.2 原因

原首次生成时间为游戏开始 **30秒**，此时各族单位已经充分游走迁移，容易远离初始栖息地。

### 19.3 优化方案

| 优化项 | 修改前 | 修改后 |
|---|---|---|
| 首次生成时间 | 30秒 | **15秒** |
| 同族支援检查 | 无 | 生成点周围3格内需≥5个同族 |
| 灭绝处理 | 强行生成在空地上 | 延迟30秒再尝试 |

### 19.4 新规则

1. 游戏开始 **15秒** 后首次尝试生成各族BOSS
2. 优先选择人口最密集的格子
3. 该格子周围 **3格** 内必须有 **≥5个同族单位** 作为"族群支援"
4. 不满足支援条件时，退回到栖息地内部+大后方
5. 若该族已灭绝或过于分散，BOSS **推迟30秒** 再尝试生成

### 19.5 代码改动索引

| 位置 | 改动 |
|---|---|
| 新增常量 | `RACE_BOSS_FIRST_SPAWN=900`（15秒） |
| `createRaceBoss(race)` | 新增周围3格内同族数量检查（≥5人） |
| `bossTick()` | 首次生成用15秒；生成失败时延迟30秒重试 |

### 19.6 预期效果

- 种族BOSS出现时，周围通常还有该族 clustering 的人群
- 即使人口在游走，15秒时仍接近初始栖息地
- 灭绝的种族不会空刷BOSS，等族群恢复后才重新苏醒

---

_最后更新：2026-06-08，随主程序 [`生态世界沙盒.html`](生态世界沙盒.html) 当前实现。_


---

## 20. 新增神力：蛊惑（2026-06-12 追加）

### 20.1 需求

解决各族抱团不进攻的问题。玩家可以通过神力强制某个单位主动攻击指定种族，打破僵局。

### 20.2 机制设计

**操作方式**：
1. 点击工具栏「🌀 蛊惑」按钮
2. 第一次点击地图：选中一个 agent（显示紫色选中圈）
3. 第二次点击地图：指定目标种族（必须是异族 agent）
4. 被选中的单位进入 **10秒蛊惑状态**

**蛊惑效果**：
- 被蛊惑单位会主动追踪 15 格内的目标种族单位
- 遇到目标种族时 **100% 强制战斗**（不受原 35% 概率限制）
- 对目标种族伤害 **+50%**
- 头顶显示紫色蛊惑标记

### 20.3 代码改动索引

| 位置 | 改动 |
|---|---|
| HTML 工具栏 | 新增 `<button class="pw" data-p="charm">🌀 蛊惑</button>` |
| 全局状态 | 新增 `charmState` |
| `applyPower` | 新增 `charm` 分支：两步选中逻辑 |
| `tick()` | 蛊惑单位主动追踪目标种族，清除 rest 状态 |
| `combatTick()` | 蛊惑单位强制战斗 + 50% 增伤 |
| `drawAgent()` | 头顶紫色蛊惑标记 |
| `draw()` | 紫色选中圈显示 |

### 20.4 事件埋点

| 事件内容 | 级别 | 触发条件 |
|:---|:---:|:---|
| XX族受到蛊惑，开始攻击 YY族！ | 警告 ⚠️ | 蛊惑成功施加 |

### 20.5 使用技巧

- 可以蛊惑一个高等级单位（如「山王」「翡翠君王」）去攻击敌族核心，造成巨大破坏
- 可以蛊惑一个濒死单位去临死反扑
- 配合「风之蹄」神器，被蛊惑单位追杀速度更快

---

_最后更新：2026-06-08，随主程序 [`生态世界沙盒.html`](生态世界沙盒.html) 当前实现。_


---

## 21. 蛊惑神力优化：范围选中幸存者（2026-06-08 追加）

### 21.1 需求改进

原蛊惑神力只能选中单个单位。用户希望改为选中一片范围内的幸存者，一次性蛊惑多个单位进攻指定种族。

### 21.2 新操作方式

1. 点击「🌀 蛊惑」按钮
2. 第一次点击/拖动：以笔刷半径 `brushR` 为范围，选中范围内所有单位
3. 第二次点击一个目标种族的单位：指定攻击目标
4. 范围内所有**非目标种族**的单位都会被蛊惑，持续10秒

### 21.3 效果示例

- 选中一片混战区域，指定人类 → 区域内的精灵/矮人/人马/海妖都会攻击人类
- 可以一次性挑起多方大战，打破抱团僵局

### 21.4 代码改动索引

| 位置 | 改动 |
|---|---|
| `applyPower` 的 `charm` 分支 | 第一步由单选改为范围选择（`brushR+0.5`） |
| `applyPower` 的 `charm` 分支 | 第二步蛊惑所有选中且不同族的单位 |
| `draw()` | 选中标记由单个圈改为多个紫色圈 |

---

_最后更新：2026-06-08，随主程序 [`生态世界沙盒.html`](生态世界沙盒.html) 当前实现。_
